# Virtual environments
.venv/
venv/
*.venv/

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# IDE
.idea/
.vscode/
*.swp
*.swo

# Git
.git/
.gitignore

# Test and coverage
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/

# Build artifacts
*.egg-info/
dist/
build/
*.egg

# Ruff/linting
.ruff_cache/

# Heavy deps (not needed in Docker)
node_modules/
target/
# …except the three host-built release binaries that Dockerfile.dev
# copies into the image. Whitelist them explicitly so the dev build
# stays fast (no in-container compile) without shipping the entire
# multi-GB target/ tree.
!target/release/ryeosd
!target/release/ryeos
!target/release/ryeos-core-tools

# Local-machine cargo config has an absolute target-dir that breaks the
# container build (cargo writes binaries to the host path instead of /build).
# CI/Docker uses cargo's default target dir under the workspace.
.cargo/

# Local development
*.env
!.env.example
