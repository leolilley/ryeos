[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "lilux-watch"
version = "0.1.0"
description = "Push-based file watcher for thread registry"

[tool.maturin]
bindings = "bin"
strip = true
