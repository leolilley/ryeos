[package]
name = "ryeos-tui-web"
version = "0.1.0"
edition = "2021"
description = "Web/WASM TUI renderer for Rye OS"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
ryeos-tui-core = { path = "../core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[target.'cfg(target_arch = "wasm32")'.dependencies]
wasm-bindgen = "0.2"
js-sys = "0.3"
web-sys = { version = "0.3", features = [
    "Window", "Document", "Element", "HtmlElement",
    "HtmlCanvasElement", "CanvasRenderingContext2d",
    "HtmlTextAreaElement", "KeyboardEvent", "MouseEvent",
    "Performance", "console",
] }
serde-wasm-bindgen = "0.6"
getrandom = { version = "0.2", features = ["js"] }

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "macros"], optional = true }
axum = { version = "0.7", optional = true }
tower-http = { version = "0.5", features = ["cors"], optional = true }

[features]
default = []
server = ["axum", "tower-http", "tokio"]

[target.'cfg(not(target_arch = "wasm32"))'.bin]
# The axum-based demo server only makes sense on native
[[bin]]
name = "ryeos-web-demo"
path = "src/bin/web-demo.rs"
required-features = ["server"]
