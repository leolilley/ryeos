[package]
name = "ryeos-vault"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
lillux = { path = "../../kernel/lillux" }
ryeos-engine = { path = "../../engine/ryeos-engine" }
toml = "0.8"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
tempfile = "3"
