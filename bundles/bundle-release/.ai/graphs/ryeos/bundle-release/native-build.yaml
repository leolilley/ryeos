# ryeos:signed:2026-09-20T14:52:10Z:3b8860807e9a7b40718505cd609b57e6814443b3817f5b52da0227d0272c821f:E7Zc/kWnETBz9AGDIdM2LKII+a6SVb45/R5liEOn+k4g5oYDzMT0OQ7xSfn/UPpKFFx5Hq3uRHxqCYkR6krCCA==:741a8bc609b398aaec0685e5aefb682faf5129a66bd192f888d23bb642c18eea
version: "1.0.0"
category: ryeos/bundle-release
description: Build exactly one non-core bundle and all payloads selected by the ownership contract into a clean captured output.
effects: live
product_recipe: config:bundle-release/native-build-products
requires:
  capabilities:
    declared:
      - ryeos.execute.tool.ryeos/bundle-release/native-build
config:
  start: build
  max_steps: 2
  on_error: fail
  config_schema:
    type: object
    additionalProperties: false
    required: [release_input]
    properties:
      release_input: {type: object}
  state: {}
  nodes:
    build:
      action:
        item_id: tool:ryeos/bundle-release/native-build
        ref_bindings: {}
        params:
          release_input: "${inputs.release_input}"
      assign: {build: "${result}"}
      next: {type: unconditional, to: done}
    done:
      node_type: return
      output: "${state.build}"
