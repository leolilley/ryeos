name: studio
version: "0.5.0"
description: "Studio surfaces, UI routes, operational inspector services, and renderer entrypoints"
requires_kinds:
  - client
  - service
  - surface
uses_kinds:
  - directive
  - graph
  - knowledge
  - tool
