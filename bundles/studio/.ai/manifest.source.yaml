name: studio
version: "0.5.0"
description: "Studio surfaces, UI routes, operational inspector services, and v0 renderer launchers"
requires_kinds:
  - client
  - service
  - surface
uses_kinds:
  - directive
  - graph
  - knowledge
  - tool
