category: "commands"
name: "submit"
kind: service
endpoint: commands.submit
required_caps: ["ryeos.execute.service.commands/submit"]
schema:
  thread_id: string
  command_type: string
  params: object?
description: "Submit a command (cancel, kill, interrupt, continue) to a thread"