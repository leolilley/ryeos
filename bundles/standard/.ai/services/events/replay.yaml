category: "events"
name: "replay"
kind: service
endpoint: events.replay
required_caps: []
schema:
  thread_id: string
  after_chain_seq: integer?
  limit: integer?
description: "Replay events for a thread (by thread_id)"