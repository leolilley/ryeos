kind: service
name: seat/list
version: "1.0.0"
description: List the operator's running seat sessions - reattach discovery, keeps the seat_session kind daemon-side
endpoint: seat.list
availability: daemon_only
