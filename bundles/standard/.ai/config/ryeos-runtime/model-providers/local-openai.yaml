# ryeos:signed:2026-07-21T00:52:25Z:7337c40314151b6078e5fbb581983aa8ad349c08d6f538a4be7d6c12e7f298c4:Kaw0xElr3UmGkJP568rddM5TELpRI4YQ91UF+5DevtY/SsDtGFu5JAzKQjthxDeGkpe+ZqOg9PsQV1CCxpOiBg==:741a8bc609b398aaec0685e5aefb682faf5129a66bd192f888d23bb642c18eea
category: "ryeos-runtime/model-providers"
family: chat_completions
# Offline/eval target: a local OpenAI-compatible server (vLLM, llama.cpp,
# etc.). `auth: {}` means no credential header is sent, so this provider
# works with internet disabled and no API key set.
base_url: "http://127.0.0.1:8000/v1"
auth: {}
headers:
  Content-Type: application/json
schemas:
  messages:
    role_map: {user: user, assistant: assistant, system: system, tool: tool}
    content_key: content
    system_message: {mode: message_role}
    tool_result:
      role: tool
      wrap_mode: direct
      block_template:
        tool_call_id: "{tool_call_id}"
        content: "{content}"
    tool_call_block_template:
      id: "{id}"
      type: function
      function:
        name: "{name}"
        arguments: "{input_json}"
  tools:
    template:
      type: function
      function:
        name: "{name}"
        description: "{description}"
        parameters: "{schema}"
  streaming: {mode: delta_merge}
body_template:
  model: "{model}"
  messages: "{messages}"
  tools: "{tools}"
  stream: "{stream}"
  stream_options:
    include_usage: true
# Local open-weights models are free to run; report zero spend so cost
# accounting still records token counts without inventing a dollar figure.
pricing:
  input_per_million: 0.0
  output_per_million: 0.0
extra:
  stream_url: "/chat/completions"
  setup:
    display_name: Local OpenAI-compatible
    priority: 90
    credential: null
    validation: {ref: "service:model-providers/validate", url: "http://127.0.0.1:8000/v1/models", timeout_seconds: 5}
    models: []
