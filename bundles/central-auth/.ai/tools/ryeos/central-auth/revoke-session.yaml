# ryeos:signed:2026-09-05T00:45:23Z:d234257392a3fd3af3acc10d3882a06526d039b1ddba613c999d165985b2bcc1:3EjWeP/gQzKNM32O+wkOqP0U3ZR46CzVSoKwvWd5tdAJHyV/eXFqVwfyexiq9DpJmaMXqldiJ5MFSGSqfLrzBQ==:741a8bc609b398aaec0685e5aefb682faf5129a66bd192f888d23bb642c18eea
version: "0.1.0"
category: "ryeos/central-auth"
name: "revoke-session"
executor_id: "@subprocess"
execution_protocol: "protocol:ryeos/core/tool_callback"
required_caps: ["ryeos.execute.tool.ryeos.central-auth.revoke-session"]
description: "Revoke one session token."

config:
  command: "python3"
  args:
    - "${tool_dir}/lib/central_auth.py"
    - "revoke-session"
  input_data: "${params_json}"
  timeout_secs: 15

config_schema:
  type: object
  properties:
    project_path:
      type: string
      description: "Project root used for .ai/state/central-auth storage."
    runtime_state_dir:
      type: string
      description: "Optional absolute central-auth runtime state dir override."
    realm_id:
      type: string
      description: "Realm/app namespace, e.g. tv-tracker."
    session_token:
      type: string
  required: [realm_id, session_token]
