# ryeos:signed:2026-07-21T08:42:53Z:1bb8761fa61e7ff1ac8b12cdbd6167caeb82c777d7f6099a641519b8b90cd093:QTWBTmJYGxBgy5MlX7iDWZnPmWsfLkqrAHDQz1+UP5UQPaW+OC5PrFOwZJbQqrGFSlb22DFnHrgx8sZ6nY4LCA==:64f806fe8f81efdecf5245e1b1941aeecfe3a56ff1826adc1214538ab69953ca
version: "0.1.0"
category: "ryeos/central-auth"
name: "verify-session"
executor_id: "@subprocess"
required_caps: ["ryeos.execute.tool.ryeos.central-auth.verify-session"]
description: "Verify a session token and optionally check one required capability."

config:
  command: "python3"
  args:
    - "{tool_dir}/lib/central_auth.py"
    - "verify-session"
  input_data: "{params_json}"
  timeout_secs: 15

config_schema:
  type: object
  properties:
    project_path:
      type: string
      description: "Project root used for .ai/state/central-auth storage."
    runtime_state_dir:
      type: string
      description: "Optional absolute central-auth runtime state dir override."
    realm_id:
      type: string
      description: "Realm/app namespace, e.g. tv-tracker."
    session_token:
      type: string
    required_capability:
      type: string
  required: [realm_id, session_token]
