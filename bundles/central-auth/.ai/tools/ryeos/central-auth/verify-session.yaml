# ryeos:signed:2026-09-05T00:45:24Z:378373f219d1f9e1b9e2f9e84f07aa380b91c92fb850c0c62ea84f8a54e2c9ce:ZfKjq9rmOMcGlmCk/bLRUPPvbHtw0blYx7SPf5e4I49TwZXEUVL0H7DaXbaT/KYJ2CNLojKBIw/mrEIjuI28Bg==:741a8bc609b398aaec0685e5aefb682faf5129a66bd192f888d23bb642c18eea
version: "0.1.0"
category: "ryeos/central-auth"
name: "verify-session"
executor_id: "@subprocess"
execution_protocol: "protocol:ryeos/core/tool_callback"
required_caps: ["ryeos.execute.tool.ryeos.central-auth.verify-session"]
description: "Verify a session token and optionally check one required capability."

config:
  command: "python3"
  args:
    - "${tool_dir}/lib/central_auth.py"
    - "verify-session"
  input_data: "${params_json}"
  timeout_secs: 15

config_schema:
  type: object
  properties:
    project_path:
      type: string
      description: "Project root used for .ai/state/central-auth storage."
    runtime_state_dir:
      type: string
      description: "Optional absolute central-auth runtime state dir override."
    realm_id:
      type: string
      description: "Realm/app namespace, e.g. tv-tracker."
    session_token:
      type: string
    required_capability:
      type: string
  required: [realm_id, session_token]
