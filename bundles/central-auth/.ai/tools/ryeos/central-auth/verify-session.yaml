# ryeos:signed:2026-07-03T03:58:05Z:5021f48a59f2fb7bf63134d436e0892e1d427f3e87e0c700a4d17465e7e97d6b:ys1chSVmyUa8EyOxBFSiJFwduyMN8zSECzTmRmrMYkGkcS2oKFd9TNVdb5OJIAnUftmHGCu5Q1ad85xp3imXDw==:741a8bc609b398aaec0685e5aefb682faf5129a66bd192f888d23bb642c18eea
version: "0.1.0"
category: "ryeos/central-auth"
name: "verify-session"
executor_id: "@subprocess"
required_caps: ["ryeos.execute.tool.ryeos.central-auth.verify-session"]
description: "Verify a session token and optionally check one required capability."

config:
  command: "python3"
  args:
    - "{tool_dir}/../../../bin/central_auth.py"
    - "verify-session"
  input_data: "{params_json}"
  timeout_secs: 15

config_schema:
  type: object
  properties:
    project_path:
      type: string
      description: "Project root used for .ai/state/central-auth storage."
    runtime_state_dir:
      type: string
      description: "Optional absolute central-auth runtime state dir override."
    realm_id:
      type: string
      description: "Realm/app namespace, e.g. tv-tracker."
    session_token:
      type: string
    required_capability:
      type: string
  required: [realm_id, session_token]
