# ryeos:signed:2026-07-14T23:11:07Z:364982f7c00d787dcb4534e4cc86db48f42b972ddc03e1373b78e15aa50caa7f:6NplKdN+MNtpOBZJiamrGnMbJk6eeWARL9qXK478bsMqW1l7RCUzspBTmHI2u63HbSyPLKzc5b8CZwVjhcZeAQ==:741a8bc609b398aaec0685e5aefb682faf5129a66bd192f888d23bb642c18eea
version: "0.1.0"
category: "ryeos/central-auth"
name: "login"
executor_id: "@subprocess"
required_caps: ["ryeos.execute.tool.ryeos.central-auth.login"]
description: "Validate credentials or redeem an invite and issue an opaque session token."

config:
  command: "python3"
  args:
    - "{tool_dir}/lib/central_auth.py"
    - "login"
  input_data: "{params_json}"
  timeout_secs: 15

config_schema:
  type: object
  properties:
    project_path:
      type: string
      description: "Project root used for .ai/state/central-auth storage."
    runtime_state_dir:
      type: string
      description: "Optional absolute central-auth runtime state dir override."
    realm_id:
      type: string
      description: "Realm/app namespace, e.g. tv-tracker."
    method:
      type: string
      enum: [passphrase, invite]
      default: passphrase
    principal_id:
      type: string
    display_name:
      type: string
      description: "Required for invite redemption."
    passphrase:
      type: string
    invite_code:
      type: string
      description: "Required for invite redemption."
    ttl_secs:
      type: integer
      default: 28800
  required: [realm_id, method]
