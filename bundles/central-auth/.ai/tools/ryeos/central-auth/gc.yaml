# ryeos:signed:2026-07-14T23:11:07Z:4faeb7eff9af68431cd62beb26d9283449db7f5966c5e8aa67d1026408291117:TiK7cRFzChv6YiEJTO5DNZKBsdxVACWQUhL4l/xcCPw58D5E6yajo5V96G7Gn3+kxHrGiXC1Rf5+GV9W5XBIBw==:741a8bc609b398aaec0685e5aefb682faf5129a66bd192f888d23bb642c18eea
version: "0.1.0"
category: "ryeos/central-auth"
name: "gc"
executor_id: "@subprocess"
required_caps: ["ryeos.execute.tool.ryeos.central-auth.gc"]
description: "Remove expired sessions and exhausted or expired invites."

config:
  command: "python3"
  args:
    - "{tool_dir}/lib/central_auth.py"
    - "gc"
  input_data: "{params_json}"
  timeout_secs: 15

config_schema:
  type: object
  properties:
    project_path:
      type: string
      description: "Project root used for .ai/state/central-auth storage."
    runtime_state_dir:
      type: string
      description: "Optional absolute central-auth runtime state dir override."
    realm_id:
      type: string
      description: "Realm/app namespace, e.g. tv-tracker."
  required: [realm_id]
