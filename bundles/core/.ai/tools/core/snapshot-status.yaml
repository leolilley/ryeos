# ryeos:signed:2026-08-11T02:28:20Z:0d2cc678c78728087279290f4d3d67404bfcbd711946e6c8cb3766cefc31b2f3:CqC8nsrOLJamTdgXelddC8Zec6NWeHcqeDmEBlE3bwWTDHDyW/4GwAktPwuRNAhZGl5UjlL7MAgyJYcq99oLBA==:741a8bc609b398aaec0685e5aefb682faf5129a66bd192f888d23bb642c18eea
version: "1.0.0"
executor_id: "@subprocess"
category: "core"
name: "snapshot-status"
required_caps: ["ryeos.read.project.live"]
requires:
  capabilities:
    manifest:
      runtime_authority:
        project_snapshots: [status]
description: "Compare the project worktree with the principal's local snapshot head. The worktree scan is timeboxed by default and returns partial results (scan_complete: false) instead of blocking."

config:
  command: "bin:ryeos-core-tools"
  args:
    - "snapshot"
    - "status"
    - "--stdin-json"
  input_data: "${params_json}"
  timeout_secs: 60

config_schema:
  type: object
  properties:
    project_path:
      type: string
      description: "Project root path"
    include_unchanged:
      type: boolean
      description: "Include unchanged files in the changes list"
      default: false
    time_budget_ms:
      type: integer
      description: "Worktree scan budget in milliseconds; 0 disables the cap and scans the full worktree"
      default: 5000
      minimum: 0
  required: [project_path]
