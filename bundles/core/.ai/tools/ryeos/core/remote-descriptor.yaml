# ryeos:signed:2026-07-27T09:00:07Z:f7e66645ada4a96c16fb858e56fb67c28d4f17ae64a32cba5c2145bf6532b874:pkL4RyzY1mU2CoUxCPYhs/2O388SO6nL7QNe7rQEA709G9wngS7rQRXjJegNKZsTF8+R9hu7n2/PpPASkkQEAg==:741a8bc609b398aaec0685e5aefb682faf5129a66bd192f888d23bb642c18eea
version: "1.0.0"
executor_id: "@subprocess"
category: "ryeos/core"
name: "remote-descriptor"
required_caps: ["ryeos.execute.tool.ryeos.core.remote-descriptor"]
description: "Export a remote descriptor trust pin for this node."

config:
  command: "bin:ryeos-core-tools"
  args:
    - "remote-descriptor"
    - "--stdin-json"
  input_data: "${params_json}"
  timeout_secs: 30

config_schema:
  type: object
  properties:
    system_space_dir:
      type: string
      description: "Node system space root. Defaults to RYEOS_APP_ROOT or the platform data dir."
    name:
      type: string
      description: "Remote name callers should use."
    url:
      type: string
      description: "Public URL callers should use to reach this node."
    capabilities:
      type: array
      items:
        type: string
      description: "Optional informational capability labels advertised by this node/provider."
      default: []
    admission_mode:
      type: string
      description: "Admission mode label to advertise. Defaults to token."
      default: "token"
    provider_name:
      type: string
      description: "Optional provider/operator label."
    output:
      type: string
      description: "Optional path to write the descriptor YAML."
  required: [name, url]
