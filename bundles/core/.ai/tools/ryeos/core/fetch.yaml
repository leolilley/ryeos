# ryeos:signed:2026-08-11T02:28:20Z:d71aae2f1e951b15d5740a5e20571ec7f5c2b455e75ee7662b41668a5945831c:6dthn5Ii+/DRvgxc7NrRkBolv/GjB/BkV7PapTNNKtE964ospoIf+mBvXvZiarP2Vbpu+LpdyeHBK0vIC+KRCg==:741a8bc609b398aaec0685e5aefb682faf5129a66bd192f888d23bb642c18eea
category: "ryeos/core"
name: "fetch"
executor_id: "@subprocess"
required_caps: ["bundle.read"]
description: "Resolve, optionally verify, and read an item via the engine"

config:
  command: "bin:ryeos-core-tools"
  args:
    - "fetch"
    - "--stdin-json"
  input_data: "${params_json}"
  timeout_secs: 60

config_schema:
  type: object
  properties:
    item_ref:
      type: string
      description: "Canonical ref to fetch (e.g. tool:ryeos/core/fetch)"
    with_content:
      type: boolean
      description: "Include the full file content in the report"
      default: false
    verify:
      type: boolean
      description: "Also verify trust status"
      default: false
    project_path:
      type: string
      description: "Project root path"
  required: [item_ref]
