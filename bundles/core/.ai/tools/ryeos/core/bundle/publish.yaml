# ryeos:signed:2026-05-23T12:06:24Z:854952b758ff76cc8f85e04c518ae029819ac00f4b8353b00ec47a0f5904cc97:jAmvT7NWS22LhBhSfdarQivb3TMTKMnCyxyGFC+RhwMa1RQALM2E/QYZTVoWZEF4dYa5RbAtGuOnpWPJlMCRDw==:741a8bc609b398aaec0685e5aefb682faf5129a66bd192f888d23bb642c18eea
category: "ryeos/core/bundle"
name: "publish"
executor_id: "@subprocess"
required_caps: []
description: "Full bundle release pipeline: bootstrap-sign, rebuild CAS, sign items, generate manifest, emit trust doc."

config:
  command: "bin:ryeos-core-tools"
  args:
    - "build"
    - "--stdin-json"
  input_data: "{params_json}"
  timeout_secs: 300

config_schema:
  type: object
  properties:
    source:
      type: string
      description: "Bundle source directory containing .ai/."
    registry_root:
      type: string
      description: "Optional registry root supplying kind schemas and parsers. Defaults to source."
    owner:
      type: string
      description: "Owner label for PUBLISHER_TRUST.toml. Defaults to local-dev."
    no_trust_doc:
      type: boolean
      description: "Suppress writing PUBLISHER_TRUST.toml."
      default: false
  required: [source]
