# ryeos:signed:2026-06-05T00:57:11Z:395009f97006149d7502fa1419cf9fc6928d08cbe2863c923b771f592c9252fb:EKzikSavNB2T58TzJx0CINRDMEl/wB9oQMFNXBtk/XV8jZ1MgNeGxQMloERYLQF5a4c6itq+Sui4obyPkJ3HAw==:741a8bc609b398aaec0685e5aefb682faf5129a66bd192f888d23bb642c18eea
category: "ryeos/core/subprocess"
version: "1.0.0"
executor_id: null
tool_type: "terminal"
description: "Internal terminal executor for @subprocess. Not directly executable; wrap it with a tool that declares executor_id: \"@subprocess\" and a config block."

# Schema for wrapper `config:` blocks consumed by @subprocess. This is
# not a direct public tool input schema; this terminal item is not
# root-executable.
config_schema:
  type: object
  properties:
    command:
      type: string
      description: Command to execute
    args:
      type: array
      items:
        type: string
      description: Command arguments
    cwd:
      type: string
      description: Working directory
    env:
      type: object
      description: Environment variables
    timeout_secs:
      type: integer
      default: 300
      description: Timeout in seconds
    input_data:
      type: string
      description: Data to pipe to stdin
  required: [command]
