# ryeos:signed:2026-08-27T08:43:19Z:364d611ccd4a455804b07b7763627b8d07e58fc79490572a5bd09ca79d865861:3oa3pBxuNTFRDPVNhHkIn0FpMfAw6IrbYNzdyqusJOheiWsSH1WbMt3pjoYu4//QLxVGfwRkpfiCJvybsWepAQ==:741a8bc609b398aaec0685e5aefb682faf5129a66bd192f888d23bb642c18eea
category: "engine/kinds/config"
version: "1.1.0"
location:
  directory: config
effective_trust:
  include_references: false
resolution: []
# Config items remain non-executable. This contract only permits an installed,
# signed config selected by another runtime to pin exact locator-free content.
content:
  external_content:
    realization_derived: effective_external_realizations
    allowed_roots: []
    max_declarations: 8
    large_content:
      max_total_bytes: 4294967296
formats:
  - extensions: [".yaml", ".yml"]
    parser: parser:ryeos/core/yaml/yaml
    signature:
      prefix: "#"
# Each config consumer enforces its own schema — config kind is an
# escape hatch for per-domain configs (execution.yaml, etc.).
# Identity composer — this kind has no multi-document composition (no extends chain, no inheritance). Items are loaded as-is.
composer: handler:ryeos/core/identity
composed_value_contract:
  root_type: mapping
  required: {}
metadata:
  rules:
    name:
      from: filename
      required: true
      match: filename
    version:
      from: path
      key: version
    description:
      from: path
      key: description
    category:
      from: path
      key: category
      required: true
      match: path
