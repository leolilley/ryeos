# ryeos:signed:2026-05-22T16:29:31Z:2d549d41efb7f38e5fd6d8133c81027ea04c5b45d080176225645beacbc26882:7zYhAdi6T8WHnwIspScnowifDRgmmpdToKp8ZnydEakkvx1v6Hecn2NukqC8Nc0HOkoY0cpPMSrbinpCMdX0BQ==:f168bc6752bd022d89a6778a8d2239b302f453d7e862770ed7ed1093c96363d1
category: "engine/kinds/config"
version: "1.0.0"
location:
  directory: config
formats:
  - extensions: [".yaml", ".yml"]
    parser: parser:ryeos/core/yaml/yaml
    signature:
      prefix: "#"
# Each config consumer enforces its own schema — config kind is an
# escape hatch for per-domain configs (execution.yaml, etc.).
# Identity composer — this kind has no multi-document composition (no extends chain, no inheritance). Items are loaded as-is.
composer: handler:ryeos/core/identity
composed_value_contract:
  root_type: mapping
  required: {}
metadata:
  rules:
    name:
      from: filename
      required: true
      match: filename
    version:
      from: path
      key: version
    description:
      from: path
      key: description
    category:
      from: path
      key: category
      required: true
      match: path
