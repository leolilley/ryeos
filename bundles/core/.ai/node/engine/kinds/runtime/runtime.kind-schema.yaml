# ryeos:signed:2026-07-23T15:25:30Z:01a0347e5037d565e65e3105fc63cc8484f68a31c94c9392ac8997ee84c84613:xgtZIfWUdn3n9bMCXzYq0BGVFm4aGY+tXRsHO6WK7j4kVn/aSxLn9C5Qc8KGnKoqLiS5rUgGSHdzd+3HZteFAw==:741a8bc609b398aaec0685e5aefb682faf5129a66bd192f888d23bb642c18eea
category: "engine/kinds/runtime"
version: "1.0.0"
location:
  directory: runtimes
effective_trust:
  include_references: false
resolution: []
execution:
  terminator:
    kind: subprocess
    protocol: protocol:ryeos/core/runtime
  thread_profile:
    name: runtime_run
    root_executable: true
    supports_interrupt: false
    supports_continuation: false
formats:
  - extensions: [".yaml", ".yml"]
    parser: parser:ryeos/core/yaml/yaml
    signature:
      prefix: "#"
# Identity composer — runtime items are loaded as-is.
composer: handler:ryeos/core/identity
composed_value_contract:
  root_type: mapping
  required:
    kind: { type: single, prim: string, enum: [runtime] }
    serves: { type: single, prim: string }
    binary_ref: { type: single, prim: string }
    abi_version: { type: single, prim: string }
    launch_contract:
      type: single
      prim: mapping
      contract:
        root_type: mapping
        required:
          primary_allowed_kinds:
            type: single
            prim: sequence
            elements: { type: single, prim: string }
          primary_allowed_spaces:
            type: single
            prim: sequence
            elements: { type: single, prim: string, enum: [bundle, project] }
          primary_allowed_trust:
            type: single
            prim: sequence
            elements:
              type: single
              prim: string
              enum: [trusted_bundle, trusted_project, untrusted_project, unsigned]
          ref_bindings: { type: single, prim: mapping }
          preparation: { type: single, prim: mapping }
          config_inputs: { type: single, prim: mapping }
          secret_policy:
            type: single
            prim: mapping
            contract:
              root_type: mapping
              required:
                max_requirements: { type: single, prim: integer }
                allowed_names:
                  type: single
                  prim: sequence
                  elements: { type: single, prim: string }
          required_runtime_data:
            type: single
            prim: sequence
            elements: { type: single, prim: string }
          runtime_facts: { type: single, prim: mapping }
          financial_authority: { type: single, prim: mapping }
  optional:
    default: { type: single, prim: boolean }
    required_caps:
      type: single
      prim: sequence
      elements: { type: single, prim: string }
    description: { type: single, prim: string }
metadata:
  rules:
    version:
      from: path
      key: version
    description:
      from: path
      key: description
    serves:
      from: path
      key: serves
    default:
      from: path
      key: default
    binary_ref:
      from: path
      key: binary_ref
    abi_version:
      from: path
      key: abi_version
    required_caps:
      from: path_string_seq
      key: required_caps
