# ryeos:signed:2026-07-02T06:11:01Z:d09ed41b84a08defa9c2b01c6f00178063f888e950e13e9009a7ef2e751de851:5qSTij9Tgijkns2RfoqPDA5OfCsX9PIgsG/CbVGnypzycONrnH8WNEQg6hTnD7zhQOT3JvY6RKlIs+pyVTyLDg==:64f806fe8f81efdecf5245e1b1941aeecfe3a56ff1826adc1214538ab69953ca
# rye:unsigned
category: "engine/kinds/runtime"
version: "1.0.0"
location:
  directory: runtimes
effective_trust:
  include_references: false
resolution: []
execution:
  terminator:
    kind: subprocess
    protocol: protocol:ryeos/core/runtime_v1
  thread_profile:
    name: runtime_run
    root_executable: true
    supports_interrupt: false
    supports_continuation: false
formats:
  - extensions: [".yaml", ".yml"]
    parser: parser:ryeos/core/yaml/yaml
    signature:
      prefix: "#"
# Identity composer — runtime items are loaded as-is.
composer: handler:ryeos/core/identity
composed_value_contract:
  root_type: mapping
  required:
    kind: { type: single, prim: string, enum: [runtime] }
    serves: { type: single, prim: string }
    binary_ref: { type: single, prim: string }
    abi_version: { type: single, prim: string }
  optional:
    default: { type: single, prim: boolean }
    required_caps:
      type: single
      prim: sequence
      elements: { type: single, prim: string }
    required_envelope_fields:
      type: single
      prim: sequence
      elements: { type: single, prim: string }
    description: { type: single, prim: string }
    schema:
      type: single
      prim: mapping
      contract:
        root_type: mapping
        required:
          envelope: { type: single, prim: string }
          result: { type: single, prim: string }
metadata:
  rules:
    version:
      from: path
      key: version
    description:
      from: path
      key: description
    serves:
      from: path
      key: serves
    default:
      from: path
      key: default
    binary_ref:
      from: path
      key: binary_ref
    abi_version:
      from: path
      key: abi_version
    required_caps:
      from: path_string_seq
      key: required_caps
    required_envelope_fields:
      from: path_string_seq
      key: required_envelope_fields
