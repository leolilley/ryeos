# ryeos:signed:2026-07-14T10:12:30Z:4bebe560dabba4b3e3e9d3a80133a06e6f972613bd9e0fd0addba3c37d31009c:CaiNUwSxiaqwUgsCyKXu+YwRPtYImDZkzuT13hXh32egM8GMOdQSmPU4wkXL0bMFA+of1jqk7nh5A+aq4fmJCg==:741a8bc609b398aaec0685e5aefb682faf5129a66bd192f888d23bb642c18eea
# rye:unsigned
category: "engine/kinds/runtime"
version: "1.0.0"
location:
  directory: runtimes
effective_trust:
  include_references: false
resolution: []
execution:
  terminator:
    kind: subprocess
    protocol: protocol:ryeos/core/runtime_v1
  thread_profile:
    name: runtime_run
    root_executable: true
    supports_interrupt: false
    supports_continuation: false
formats:
  - extensions: [".yaml", ".yml"]
    parser: parser:ryeos/core/yaml/yaml
    signature:
      prefix: "#"
# Identity composer — runtime items are loaded as-is.
composer: handler:ryeos/core/identity
composed_value_contract:
  root_type: mapping
  required:
    kind: { type: single, prim: string, enum: [runtime] }
    serves: { type: single, prim: string }
    binary_ref: { type: single, prim: string }
    abi_version: { type: single, prim: string }
  optional:
    default: { type: single, prim: boolean }
    required_caps:
      type: single
      prim: sequence
      elements: { type: single, prim: string }
    required_envelope_fields:
      type: single
      prim: sequence
      elements: { type: single, prim: string }
    description: { type: single, prim: string }
metadata:
  rules:
    version:
      from: path
      key: version
    description:
      from: path
      key: description
    serves:
      from: path
      key: serves
    default:
      from: path
      key: default
    binary_ref:
      from: path
      key: binary_ref
    abi_version:
      from: path
      key: abi_version
    required_caps:
      from: path_string_seq
      key: required_caps
    required_envelope_fields:
      from: path_string_seq
      key: required_envelope_fields
