# ryeos:signed:2026-05-24T11:43:14Z:de5ac3bffed83e5b6af12b91b2a940e8eee550a1fddd21a9ab20059177a197af:l1Y6phTd2/iOwy33dMX5q2uEfZK8xJ99IQPJqskYwxyhEUYINSMQKec8gZBowTfUAqs8SAVr0lkpw8BNtEqmDg==:741a8bc609b398aaec0685e5aefb682faf5129a66bd192f888d23bb642c18eea
# rye:unsigned
category: "engine/kinds/runtime"
version: "1.0.0"
location:
  directory: runtimes
effective_trust:
  include_references: false
resolution: []
execution:
  terminator:
    kind: subprocess
    protocol: protocol:ryeos/core/runtime_v1
  thread_profile:
    name: runtime_run
    root_executable: true
    supports_interrupt: false
    supports_continuation: false
formats:
  - extensions: [".yaml", ".yml"]
    parser: parser:ryeos/core/yaml/yaml
    signature:
      prefix: "#"
# Identity composer — runtime items are loaded as-is.
composer: handler:ryeos/core/identity
composed_value_contract:
  root_type: mapping
  required:
    kind: { type: single, prim: string, enum: [runtime] }
    serves: { type: single, prim: string }
    binary_ref: { type: single, prim: string }
    abi_version: { type: single, prim: string }
  optional:
    default: { type: single, prim: boolean }
    required_caps:
      type: single
      prim: sequence
      elements: { type: single, prim: string }
    description: { type: single, prim: string }
    schema:
      type: single
      prim: mapping
      contract:
        root_type: mapping
        required:
          envelope: { type: single, prim: string }
          result: { type: single, prim: string }
metadata:
  rules:
    version:
      from: path
      key: version
    description:
      from: path
      key: description
    serves:
      from: path
      key: serves
    default:
      from: path
      key: default
    binary_ref:
      from: path
      key: binary_ref
    abi_version:
      from: path
      key: abi_version
    required_caps:
      from: path_string_seq
      key: required_caps
