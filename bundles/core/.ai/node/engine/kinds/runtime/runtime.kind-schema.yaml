# ryeos:signed:2026-07-15T00:27:45Z:6adf3a85ed3935594f1148e6be225c2c9c2ec113ce63937ab4e5466d1fe4c071:BbMF8n0vUR2vEmLOCiiEccILXOGmwT0OFAGdkI8owf2A+fZ+uD6uBHWlHIbdx5HH60umDNmzBa8UOFRkh7AvBw==:741a8bc609b398aaec0685e5aefb682faf5129a66bd192f888d23bb642c18eea
# rye:unsigned
category: "engine/kinds/runtime"
version: "1.0.0"
location:
  directory: runtimes
effective_trust:
  include_references: false
resolution: []
execution:
  terminator:
    kind: subprocess
    protocol: protocol:ryeos/core/runtime_v1
  thread_profile:
    name: runtime_run
    root_executable: true
    supports_interrupt: false
    supports_continuation: false
formats:
  - extensions: [".yaml", ".yml"]
    parser: parser:ryeos/core/yaml/yaml
    signature:
      prefix: "#"
# Identity composer — runtime items are loaded as-is.
composer: handler:ryeos/core/identity
composed_value_contract:
  root_type: mapping
  required:
    kind: { type: single, prim: string, enum: [runtime] }
    serves: { type: single, prim: string }
    binary_ref: { type: single, prim: string }
    abi_version: { type: single, prim: string }
    launch_contract:
      type: single
      prim: mapping
      contract:
        root_type: mapping
        required:
          primary_allowed_kinds:
            type: single
            prim: sequence
            elements: { type: single, prim: string }
          primary_allowed_spaces:
            type: single
            prim: sequence
            elements: { type: single, prim: string, enum: [bundle, project] }
          primary_allowed_trust:
            type: single
            prim: sequence
            elements:
              type: single
              prim: string
              enum: [trusted_bundle, trusted_project, untrusted_project, unsigned]
          ref_bindings: { type: single, prim: mapping }
          preparation: { type: single, prim: mapping }
          config_inputs: { type: single, prim: mapping }
          secret_policy:
            type: single
            prim: mapping
            contract:
              root_type: mapping
              required:
                max_requirements: { type: single, prim: integer }
                allowed_names:
                  type: single
                  prim: sequence
                  elements: { type: single, prim: string }
          required_runtime_data:
            type: single
            prim: sequence
            elements: { type: single, prim: string }
          runtime_facts: { type: single, prim: mapping }
  optional:
    default: { type: single, prim: boolean }
    required_caps:
      type: single
      prim: sequence
      elements: { type: single, prim: string }
    description: { type: single, prim: string }
    schema:
      type: single
      prim: mapping
      contract:
        root_type: mapping
        required:
          envelope: { type: single, prim: string }
          result: { type: single, prim: string }
metadata:
  rules:
    version:
      from: path
      key: version
    description:
      from: path
      key: description
    serves:
      from: path
      key: serves
    default:
      from: path
      key: default
    binary_ref:
      from: path
      key: binary_ref
    abi_version:
      from: path
      key: abi_version
    required_caps:
      from: path_string_seq
      key: required_caps
