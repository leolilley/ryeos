# ryeos:signed:2026-08-09T08:48:12Z:d219e345f811532e4084c903b95b2c8e0396d3d247dd6c20a3efe593631a4268:7kSizjhz63OfTtHmEIoqCBY4El/5kKG4zD9pNlfT8fb+aJJK8BdMKIiJhYJLMW/QA1HtRWiZpEadMXDCQKA8BQ==:741a8bc609b398aaec0685e5aefb682faf5129a66bd192f888d23bb642c18eea
category: "engine/kinds/worker"
version: "1.0.0"
location:
  directory: workers
  excluded_directories: [lib]
effective_trust:
  include_references: false
resolution: []
execution:
  terminator:
    kind: subprocess
    protocol: protocol:ryeos/core/persistent_session
  aliases:
    "@subprocess": "tool:ryeos/core/subprocess/execute"
  persistent_session:
    target_path: [supported_target]
    max_processes: 1
    max_inflight_per_process: 1
    max_address_space_bytes: 17179869184
    max_cpu_seconds: 3600
    real_uid_process_limit: 512
    ready_timeout_ms: 600000
    request_timeout_ms: 3600000
    idle_timeout_ms: 1800000
  external_content:
    realization_derived: effective_external_realizations
    allowed_roots: ["bundle:own"]
    max_declarations: 8
    large_content:
      max_total_bytes: 4294967296
formats:
  - extensions: [".yaml", ".yml"]
    parser: parser:ryeos/core/yaml/yaml
    signature:
      prefix: "#"
inventory_policy:
  admission:
    capability_template: "ryeos.execute.{kind}.{bare_id}"
  required_metadata: [executor_id]
composer: handler:ryeos/core/identity
composed_value_contract:
  root_type: mapping
  required:
    category: {type: single, prim: string}
    version: {type: single, prim: string}
    executor_id: {type: single, prim: string}
    config: {type: single, prim: mapping}
    external_content: {type: single, prim: array}
    supported_target: {type: single, prim: mapping}
  optional:
    description: {type: single, prim: string}
runtime:
  handlers:
    - type: config
    - type: env_config
    - type: config_resolve
    - type: verify_deps
    - type: execution_params
  ignored_keys:
    - category
    - version
    - description
    - executor_id
    - external_content
    - supported_target
metadata:
  rules:
    name:
      from: filename
      required: true
      match: filename
    version:
      from: path
      key: version
    category:
      from: path
      key: category
      required: true
      match: path
    description:
      from: path
      key: description
    executor_id:
      from: path
      key: executor_id
