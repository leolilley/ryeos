# rye:signed:2026-03-16T09:27:24Z:c8ce530a87bbef587727d09401b13c12c525e09dacc910041cf7bd5421c92e6a:gO24hqlW3t0jE_pmoQZu2DFcS8bS0KR5ZeXGx8_2vByWgCGMgfS7owgSR9HowLyecjmGGigxYZL87EFcnrh3Cg==:4b987fd4e40303ac
version: "1.0.0"
tool_type: runtime
executor_id: rye/core/primitives/subprocess
category: rye/core/runtimes/mcp
description: "MCP stdio runtime - executes MCP tools via stdio transport"

env_config:
  env:
    PYTHONUNBUFFERED: "1"

config:
  command: "${RYE_KERNEL_PYTHON}"
  args:
    - "{system_space}/.ai/tools/rye/mcp/connect.py"
    - "--server-config"
    - "{server_config_path}"
    - "--tool"
    - "{tool_name}"
    - "--project-path"
    - "{project_path}"
  input_data: "{params_json}"
  timeout: 60

config_schema:
  type: object
  properties:
    server:
      type: string
      description: "Relative tool ID to server config (e.g., mcp/servers/rye-os)"
    tool_name:
      type: string
      description: MCP tool name to call
    timeout:
      type: number
      description: Execution timeout in seconds
      default: 60
  required: [server, tool_name]
