# rye:signed:2026-04-20T05:37:46Z:19ef1835c63e42f493ed7d92208e5677824a934f6fdeb2c0235c1842e105a2d4:det_hJutk0_6MbUnr_4JkhSLoghJhWFD76eUV0viOa3JbWNFO635lwb1r-7ZWe1rOZdvX0haTykfLrNSIBylBw:4b987fd4e40303ac
# rye:unsigned
version: "1.0.0"
tool_type: runtime
executor_id: rye/core/primitives/execute
category: rye/core/runtimes/mcp
description: "MCP stdio runtime - executes MCP tools via stdio transport"

env_config:
  env:
    PYTHONUNBUFFERED: "1"

config_resolve:
  path: "execution/execution.yaml"
  mode: "deep_merge"

config:
  command: "${RYE_KERNEL_PYTHON}"
  args:
    - "{system_space}/.ai/tools/rye/mcp/connect.py"
    - "--server-config"
    - "{server_config_path}"
    - "--tool"
    - "{tool_name}"
    - "--project-path"
    - "{project_path}"
  input_data: "{params_json}"
  timeout: 60

config_schema:
  type: object
  properties:
    server:
      type: string
      description: "Relative tool ID to server config (e.g., mcp/servers/rye-os)"
    tool_name:
      type: string
      description: MCP tool name to call
    timeout:
      type: number
      description: Execution timeout in seconds
      default: 60
  required: [server, tool_name]
