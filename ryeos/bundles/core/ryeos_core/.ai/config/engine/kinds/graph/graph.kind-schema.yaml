# rye:signed:2026-04-27T16:16:31Z:296e769276ba4a14c28fd9fb235ffa41ed23a9c5db38ddb467809b5a6e8f470f:5/LhsvYw5e0Sm+jxehbYJWBvp06XqPrYf66aju6gep+WoUh4U449MjRcH2gjcO6DGB9qtd5yYc7kCEKWyhDcBg==:8f4c002347bcb25b80e32a9f5ba7064638f0d372b8dd5cfbff3da765f94ef4bb
kind: graph
category: "engine/kinds/graph"
version: "1.0.0"
location:
  directory: graphs
execution:
  delegate:
    via: runtime_registry
  thread_profile: graph_run
  resolution:
    - step: resolve_extends_chain
formats:
  - extensions: [".yaml", ".yml"]
    parser: parser:rye/core/yaml/yaml
    signature:
      prefix: "#"
# Graph permissions composer — lifts `permissions:` from the graph item
# into `policy_facts.effective_caps` so the daemon-side callback cap
# enforcement (V5.5 P2) gates graph action callbacks the same way it
# gates directive tool callbacks. Replaces the identity composer which
# produced no policy facts (deny-all at the daemon boundary).
composer: rye/core/graph_permissions
composed_value_contract:
  root_type: mapping
  required: {}
  optional:
    config: mapping
    permissions: sequence
metadata:
  rules:
    name:
      from: filename
    version:
      from: path
      key: version
    description:
      from: path
      key: description
    category:
      from: path
      key: category
    executor_id:
      from: path
      key: executor_id