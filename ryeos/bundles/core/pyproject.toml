[project]
name = "ryeos-core"
version = "0.1.0"
description = "RYE Core - minimal bundle with core runtimes, primitives, and extractors"

dependencies = [
    "lilux",
    "pyyaml",
    "cryptography",
    "packaging>=21.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.0",
]

[project.entry-points."rye.bundles"]
ryeos-core = "rye.bundle_entrypoints:get_ryeos_core_bundle"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
# Ship the same rye module from the sibling rye/ directory
packages = ["../rye/rye"]

[tool.hatch.build]
exclude = [
    ".venv",
    "__pycache__",
    "*.pyc",
]

[tool.hatch.build.targets.wheel.force-include]
"../rye/rye/.ai/tools/rye/core" = "rye/.ai/tools/rye/core"
"../rye/rye/.ai/directives/rye/core" = "rye/.ai/directives/rye/core"
"../rye/rye/.ai/knowledge/rye/core" = "rye/.ai/knowledge/rye/core"
"../rye/rye/.ai/bundles" = "rye/.ai/bundles"
