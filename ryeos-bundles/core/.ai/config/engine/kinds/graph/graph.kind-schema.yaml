# rye:signed:2026-04-26T10:34:13Z:0ebc66d5484bf6c272cf785791d6f810c9485abf849766c984bd4244f37a1a9e:0ilOq4Ww3Bkd5wcVOinHnAvYyVUiBRg/Szm913VNxGuJwKLu37ix2PzdXUC0fzERYAIyp0FH6H6m/fb0FFUuBw==:8f4c002347bcb25b80e32a9f5ba7064638f0d372b8dd5cfbff3da765f94ef4bb
category: "engine/kinds/graph"
version: "1.0.0"
location:
  directory: graphs
execution:
  # No `@graph` alias — graph→runtime dispatch is resolved by
  # `RuntimeRegistry::lookup_for("graph")` in the dispatch loop.
  # This is an EXPLICIT, NAMED registry hop (see B2 in the V5.4
  # advanced-path foundation), not a silent fallback.
  thread_profile: graph_run
  resolution:
    - step: resolve_extends_chain
formats:
  - extensions: [".yaml", ".yml"]
    parser: parser:rye/core/yaml/yaml
    signature:
      prefix: "#"
# Empty until graph schema stabilizes — graph-walker enforces fields
# at consumer layer for now.
# Identity composer — this kind has no multi-document composition (no extends chain, no inheritance). Items are loaded as-is.
composer: rye/core/identity
composed_value_contract:
  root_type: mapping
  required: {}
metadata:
  rules:
    name:
      from: filename
    version:
      from: path
      key: version
    description:
      from: path
      key: description
    category:
      from: path
      key: category
    executor_id:
      from: path
      key: executor_id