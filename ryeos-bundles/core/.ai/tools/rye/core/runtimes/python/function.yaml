# rye:signed:2026-04-26T09:49:05Z:6dd3c2a8ac15ff344da444f659f2f4220dbd70d7ee3b0ff1b2c8a50cd8c1263b:NQ0ypLGDgWh+JEiF+PS0EGdGoWAf3VsxoRcmwdIOZbNSxp31an2NLC9FnaNS3S3G0tg/R1nqBzLyb6gxxhdoDg==:8f4c002347bcb25b80e32a9f5ba7064638f0d372b8dd5cfbff3da765f94ef4bb
version: "1.0.0"
__executor_id__: "@subprocess"
category: rye/core/runtimes/python
description: "Python function runtime - calls execute(params, project_path) and serializes result to stdout"

env_config:
  interpreter:
    type: local_binary
    binary: python
    candidates: [python3]
    search_paths: [".venv/bin", ".venv/Scripts"]
    var: RYE_PYTHON
    path_candidates: [python3]
  env:
    PYTHONUNBUFFERED: "1"
    PROJECT_VENV_PYTHON: "{interpreter}"
  env_paths:
    PYTHONPATH:
      prepend: ["{tool_dir}", "{runtime_dir}/lib"]

verify_deps:
  enabled: true
  scope: tool_dir
  recursive: true
  extensions: [".py", ".yaml", ".yml", ".json"]
  exclude_dirs: ["__pycache__", ".venv", "node_modules", ".git", "config"]

config_resolve:
  path: "execution/execution.yaml"
  mode: "deep_merge"

config:
  command: "{interpreter}"
  args:
    - "-c"
    - |
      import sys,json,importlib.util,inspect,asyncio
      spec=importlib.util.spec_from_file_location("tool",sys.argv[1])
      mod=importlib.util.module_from_spec(spec)
      spec.loader.exec_module(mod)
      fn=getattr(mod,"execute",None)
      if not fn:sys.exit("No execute() in "+sys.argv[1])
      p=json.loads(sys.stdin.read())
      if inspect.iscoroutinefunction(fn):r=asyncio.run(fn(p,sys.argv[2]))
      else:r=fn(p,sys.argv[2])
      print(json.dumps(r,default=str)if r is not None else'{}')
    - "{tool_path}"
    - "{project_path}"
  input_data: "{params_json}"
  timeout_secs: 300

config_schema:
  type: object
  properties:
    script:
      type: string
      description: Python module with execute(params, project_path) function