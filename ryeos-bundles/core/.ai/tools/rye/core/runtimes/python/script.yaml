# rye:signed:2026-05-01T09:45:28Z:78755325bfb7c66e3de58c85a7c1e5055078d38186f3a8bdf28021cd05888538:diTWwcc8GZGHzA6FD1irzFtg/v5uPYML1jjlWWxBkHBfgkimxG6r8UZUfDQ46fdBN9f9so60YN4dPxyt9BDYDA==:09674c8998e9dd01bfc40ec9f8c4b6b2c1bd01333842582a9c34b3c7db5aa86c
category: "rye/core/runtimes/python"
version: "1.0.0"
executor_id: "@subprocess"
description: "Python script runtime - runs Python scripts that manage their own __main__ entry point"

env_config:
  interpreter:
    type: local_binary
    binary: python
    candidates: [python3]
    search_paths: [".venv/bin", ".venv/Scripts"]
    var: RYE_PYTHON
    path_candidates: [python3]
  env:
    PYTHONUNBUFFERED: "1"
    PROJECT_VENV_PYTHON: "{interpreter}"
  env_paths:
    PYTHONPATH:
      prepend: ["{tool_dir}", "{runtime_dir}/lib"]

verify_deps:
  enabled: true
  scope: tool_dir
  recursive: true
  extensions: [".py", ".yaml", ".yml", ".json"]
  exclude_dirs: ["__pycache__", ".venv", "node_modules", ".git", "config"]

config_resolve:
  path: "execution/execution.yaml"
  mode: "deep_merge"

config:
  command: "{interpreter}"
  args:
    - "{tool_path}"
    - "--project-path"
    - "{project_path}"
  input_data: "{params_json}"
  timeout_secs: 300

config_schema:
  type: object
  properties:
    script:
      type: string
      description: Python script path or inline code
    args:
      type: array
      items:
        type: string
      description: Script arguments
    module:
      type: string
      description: "Module to run with -m flag"