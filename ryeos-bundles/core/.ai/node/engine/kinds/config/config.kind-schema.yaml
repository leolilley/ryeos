# rye:signed:2026-05-05T07:56:52Z:e707807545c8a454d9f817fd97dc548d6a53b5868b8be07546c81a976ab88a7f:NgiB4CKjaz9ZuhqJf4g8Sh+ZiOWGyk0DnaNPoMp25bjz8tN+mljwtx/1BB1p/SBsxEqbY1O5zUe2QGgywH65BA==:09674c8998e9dd01bfc40ec9f8c4b6b2c1bd01333842582a9c34b3c7db5aa86c
category: "engine/kinds/config"
version: "1.0.0"
location:
  directory: config
formats:
  - extensions: [".yaml", ".yml"]
    parser: parser:rye/core/yaml/yaml
    signature:
      prefix: "#"
# Each config consumer enforces its own schema — config kind is an
# escape hatch for per-domain configs (execution.yaml, etc.).
# Identity composer — this kind has no multi-document composition (no extends chain, no inheritance). Items are loaded as-is.
composer: handler:rye/core/identity
composed_value_contract:
  root_type: mapping
  required: {}
metadata:
  rules:
    name:
      from: filename
      required: true
      match: filename
    version:
      from: path
      key: version
    description:
      from: path
      key: description
    category:
      from: path
      key: category
      required: true
      match: path
