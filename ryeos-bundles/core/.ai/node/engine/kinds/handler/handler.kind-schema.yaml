# ryeos:signed:2026-05-10T01:54:14Z:7da35f7b39b164a67e1867c7503a9d114f13fbdb24217fdb09db5510e9ec6549:icef39mcfx8nFXvm89QkM6/wdfI3IbRjHMLfXCEkbRly3Adzc7VAOX3rrxfpUqnph6KhoEP+JYiUVOd6ALfoBQ==:741a8bc609b398aaec0685e5aefb682faf5129a66bd192f888d23bb642c18eea
category: "engine/kinds/handler"
version: "1.0.0"
location:
  directory: handlers
formats:
  - extensions: [".yaml", ".yml"]
    parser: parser:ryeos/core/yaml/yaml
    signature:
      prefix: "#"
# Handler descriptors are NOT root-executable. They are an
# engine-internal extension-point registry (parsers + composers in v1).
# Therefore: NO `execution:` block, NO terminator. The handler kind is
# config-only metadata.
#
# Items of this kind are loaded by the Layer-1 raw signed-YAML loader
# (HandlerRegistry::load_base) BEFORE parser/composer dispatch is
# wired up. This breaks the bootstrap cycle.
composer: handler:ryeos/core/identity
composed_value_contract:
  root_type: mapping
  required: {}
metadata:
  rules:
    name:
      from: path
      key: name
      required: true
      match: filename
    version:
      from: path
      key: version
    category:
      from: path
      key: category
      required: true
      match: path
    description:
      from: path
      key: description
    serves:
      from: path
      key: serves
      required: true
    binary_ref:
      from: path
      key: binary_ref
      required: true
    abi_version:
      from: path
      key: abi_version
      required: true
    required_caps:
      from: path_string_seq
      key: required_caps
